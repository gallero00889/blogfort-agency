<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Agente BlogFort · Content Intelligence</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Share+Tech+Mono&family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@700;900&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0c10;
    --surface: #111318;
    --surface2: #181b22;
    --border: #1e2330;
    --border2: #252a38;
    --accent: #00a8ff;
    --accent2: #0077cc;
    --gold: #f0a500;
    --green: #00c896;
    --red: #ff4b6e;
    --purple: #a855f7;
    --text: #e8eaf0;
    --text2: #8892a4;
    --text3: #4a5568;
    --mono: 'Share Tech Mono', monospace;
    --sans: 'Inter', sans-serif;
    --serif: 'Playfair Display', serif;
    --radius: 8px;
    --glow: 0 0 20px rgba(0,168,255,0.15);
  }

  * { margin:0; padding:0; box-sizing:border-box; }
  html, body { height:100%; background:var(--bg); color:var(--text); font-family:var(--sans); overflow:hidden; }

  /* TOPBAR */
  .topbar {
    height: 54px;
    background: var(--surface);
    border-bottom: 1px solid var(--border);
    display: flex;
    align-items: center;
    padding: 0 16px;
    gap: 12px;
    flex-shrink: 0;
    position: relative;
  }
  .topbar-brand {
    font-family: var(--serif);
    font-size: 18px;
    font-weight: 900;
    color: #fff;
    letter-spacing: -0.5px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .topbar-brand em { color: var(--gold); font-style: normal; }
  .agent-badge {
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 2px;
    color: var(--accent);
    background: rgba(0,168,255,0.08);
    border: 1px solid rgba(0,168,255,0.2);
    border-radius: 3px;
    padding: 3px 8px;
    text-transform: uppercase;
  }
  .topbar-spacer { flex: 1; }
  .status-dot {
    width: 7px; height: 7px;
    border-radius: 50%;
    background: var(--green);
    box-shadow: 0 0 8px var(--green);
    animation: pulse 2s infinite;
  }
  @keyframes pulse { 0%,100%{opacity:1} 50%{opacity:.4} }
  .status-lbl {
    font-family: var(--mono);
    font-size: 9px;
    color: var(--green);
    letter-spacing: 1.5px;
  }
  .api-btn {
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 1.5px;
    color: var(--text2);
    background: var(--surface2);
    border: 1px solid var(--border2);
    border-radius: 4px;
    padding: 5px 10px;
    cursor: pointer;
    transition: all .2s;
  }
  .api-btn:hover { color: var(--accent); border-color: var(--accent); }

  /* LAYOUT */
  .shell { display:flex; height:calc(100vh - 54px); }

  /* SIDEBAR */
  .sidebar {
    width: 220px;
    flex-shrink: 0;
    background: var(--surface);
    border-right: 1px solid var(--border);
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }
  .sidebar-section {
    padding: 14px 12px 6px;
    font-family: var(--mono);
    font-size: 8px;
    letter-spacing: 2px;
    color: var(--text3);
    text-transform: uppercase;
  }
  .nav-item {
    display: flex;
    align-items: center;
    gap: 9px;
    padding: 9px 14px;
    cursor: pointer;
    border-radius: 0;
    transition: all .15s;
    border-left: 2px solid transparent;
    font-size: 12px;
    color: var(--text2);
    position: relative;
  }
  .nav-item:hover { background: rgba(255,255,255,0.03); color: var(--text); }
  .nav-item.active {
    background: rgba(0,168,255,0.06);
    border-left-color: var(--accent);
    color: var(--accent);
  }
  .nav-icon { font-size: 14px; width: 18px; text-align: center; }
  .nav-label { font-size: 11px; font-weight: 500; }
  .nav-count {
    margin-left: auto;
    font-family: var(--mono);
    font-size: 8px;
    color: var(--text3);
    background: var(--surface2);
    border-radius: 10px;
    padding: 1px 6px;
  }
  .sidebar-footer {
    margin-top: auto;
    padding: 12px;
    border-top: 1px solid var(--border);
  }
  .quota-bar-wrap { margin-bottom: 6px; }
  .quota-lbl { font-family:var(--mono); font-size:8px; color:var(--text3); letter-spacing:1px; margin-bottom:4px; }
  .quota-bar { height:3px; background:var(--border2); border-radius:2px; overflow:hidden; }
  .quota-fill { height:100%; background: linear-gradient(90deg, var(--accent), var(--purple)); border-radius:2px; transition:width .4s; }

  /* MAIN */
  .main { flex:1; display:flex; flex-direction:column; overflow:hidden; }

  /* CHAT AREA */
  .chat-area {
    flex: 1;
    overflow-y: auto;
    padding: 20px 24px;
    display: flex;
    flex-direction: column;
    gap: 16px;
    scroll-behavior: smooth;
  }
  .chat-area::-webkit-scrollbar { width:4px; }
  .chat-area::-webkit-scrollbar-thumb { background:var(--border2); border-radius:2px; }

  /* MESSAGES */
  .msg { display:flex; gap:10px; max-width:820px; }
  .msg.user { flex-direction:row-reverse; align-self:flex-end; }
  .msg-avatar {
    width: 32px; height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 14px;
    flex-shrink: 0;
    border: 1px solid var(--border2);
  }
  .msg.agent .msg-avatar { background: rgba(0,168,255,0.1); border-color: rgba(0,168,255,0.2); }
  .msg.user .msg-avatar { background: rgba(240,165,0,0.1); border-color: rgba(240,165,0,0.2); }
  .msg-bubble {
    padding: 12px 16px;
    border-radius: var(--radius);
    font-size: 13px;
    line-height: 1.6;
    max-width: calc(100% - 44px);
  }
  .msg.agent .msg-bubble {
    background: var(--surface);
    border: 1px solid var(--border);
    color: var(--text);
    border-radius: 2px 8px 8px 8px;
  }
  .msg.user .msg-bubble {
    background: rgba(0,168,255,0.1);
    border: 1px solid rgba(0,168,255,0.2);
    color: var(--text);
    border-radius: 8px 2px 8px 8px;
  }
  .msg-meta {
    font-family: var(--mono);
    font-size: 8px;
    color: var(--text3);
    letter-spacing: 1px;
    margin-bottom: 5px;
  }
  .msg-content { white-space: pre-wrap; word-break: break-word; }
  .msg-content strong { color: var(--accent); font-weight: 600; }
  .msg-content em { color: var(--gold); font-style: normal; font-weight: 500; }
  .msg-content code {
    font-family: var(--mono);
    background: var(--surface2);
    border: 1px solid var(--border2);
    border-radius: 3px;
    padding: 1px 5px;
    font-size: 11px;
    color: var(--green);
  }

  /* OUTPUT BLOCKS */
  .output-block {
    background: var(--surface2);
    border: 1px solid var(--border2);
    border-radius: var(--radius);
    margin-top: 10px;
    overflow: hidden;
  }
  .output-header {
    padding: 8px 12px;
    background: rgba(0,168,255,0.05);
    border-bottom: 1px solid var(--border2);
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 1.5px;
    color: var(--accent);
  }
  .output-body {
    padding: 14px;
    font-size: 13px;
    line-height: 1.7;
    color: var(--text);
    white-space: pre-wrap;
    word-break: break-word;
  }
  .output-actions {
    padding: 8px 12px;
    border-top: 1px solid var(--border2);
    display: flex;
    gap: 6px;
  }
  .out-btn {
    font-family: var(--mono);
    font-size: 8px;
    letter-spacing: 1px;
    padding: 4px 10px;
    border-radius: 3px;
    border: 1px solid var(--border2);
    background: transparent;
    color: var(--text2);
    cursor: pointer;
    transition: all .15s;
  }
  .out-btn:hover { color: var(--accent); border-color: var(--accent); }
  .out-btn.primary { background: rgba(0,168,255,0.1); border-color: rgba(0,168,255,0.3); color: var(--accent); }

  /* SEO SCORE */
  .seo-score-strip {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin-top: 8px;
  }
  .seo-pill {
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 1px;
    padding: 3px 8px;
    border-radius: 10px;
    border: 1px solid;
  }
  .seo-pill.green { color: var(--green); border-color: rgba(0,200,150,.3); background: rgba(0,200,150,.07); }
  .seo-pill.yellow { color: var(--gold); border-color: rgba(240,165,0,.3); background: rgba(240,165,0,.07); }
  .seo-pill.red { color: var(--red); border-color: rgba(255,75,110,.3); background: rgba(255,75,110,.07); }

  /* QUICK ACTIONS */
  .quick-actions {
    padding: 12px 24px;
    border-top: 1px solid var(--border);
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
    background: var(--surface);
  }
  .qa-btn {
    font-family: var(--mono);
    font-size: 8px;
    letter-spacing: 1px;
    padding: 5px 10px;
    border-radius: 4px;
    border: 1px solid var(--border2);
    background: var(--surface2);
    color: var(--text2);
    cursor: pointer;
    transition: all .15s;
    white-space: nowrap;
  }
  .qa-btn:hover { border-color: var(--accent); color: var(--accent); background: rgba(0,168,255,0.05); }

  /* INPUT */
  .input-area {
    padding: 12px 24px 16px;
    background: var(--bg);
    border-top: 1px solid var(--border);
  }
  .input-wrap {
    display: flex;
    gap: 8px;
    background: var(--surface);
    border: 1px solid var(--border2);
    border-radius: var(--radius);
    padding: 10px 14px;
    transition: border-color .2s;
  }
  .input-wrap:focus-within { border-color: rgba(0,168,255,0.4); box-shadow: var(--glow); }
  .chat-input {
    flex: 1;
    background: transparent;
    border: none;
    outline: none;
    color: var(--text);
    font-family: var(--sans);
    font-size: 13px;
    resize: none;
    max-height: 120px;
    line-height: 1.5;
  }
  .chat-input::placeholder { color: var(--text3); }
  .send-btn {
    background: var(--accent2);
    border: none;
    border-radius: 5px;
    width: 34px;
    height: 34px;
    color: #fff;
    cursor: pointer;
    font-size: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all .2s;
    flex-shrink: 0;
    align-self: flex-end;
  }
  .send-btn:hover { background: var(--accent); transform: scale(1.05); }
  .send-btn:disabled { opacity:.4; cursor:not-allowed; transform:none; }
  .input-hint {
    font-family: var(--mono);
    font-size: 8px;
    color: var(--text3);
    letter-spacing: 1px;
    margin-top: 5px;
    text-align: center;
  }

  /* MODO BADGES */
  .mode-selector {
    display: flex;
    gap: 4px;
    margin-bottom: 8px;
    flex-wrap: wrap;
  }
  .mode-pill {
    font-family: var(--mono);
    font-size: 8px;
    letter-spacing: 1px;
    padding: 4px 8px;
    border-radius: 3px;
    border: 1px solid var(--border2);
    background: var(--surface2);
    color: var(--text3);
    cursor: pointer;
    transition: all .15s;
  }
  .mode-pill.active { border-color: var(--accent); color: var(--accent); background: rgba(0,168,255,0.08); }
  .mode-pill:hover:not(.active) { color: var(--text2); }

  /* TYPING INDICATOR */
  .typing { display:flex; gap:4px; align-items:center; padding:8px 0; }
  .typing span {
    width:6px; height:6px; border-radius:50%;
    background: var(--accent);
    animation: blink 1.2s infinite;
  }
  .typing span:nth-child(2){animation-delay:.2s}
  .typing span:nth-child(3){animation-delay:.4s}
  @keyframes blink{0%,100%{opacity:.2}50%{opacity:1}}

  /* MODAL API */
  .modal-overlay {
    display: none;
    position: fixed; inset:0;
    background: rgba(0,0,0,.7);
    z-index: 100;
    align-items: center;
    justify-content: center;
  }
  .modal-overlay.on { display:flex; }
  .modal {
    background: var(--surface);
    border: 1px solid var(--border2);
    border-radius: 10px;
    padding: 24px;
    width: 360px;
    max-width: 90vw;
  }
  .modal-title {
    font-family: var(--mono);
    font-size: 11px;
    letter-spacing: 2px;
    color: var(--accent);
    margin-bottom: 16px;
    text-transform: uppercase;
  }
  .modal-input {
    width: 100%;
    background: var(--surface2);
    border: 1px solid var(--border2);
    border-radius: 5px;
    padding: 10px 12px;
    color: var(--text);
    font-family: var(--mono);
    font-size: 11px;
    outline: none;
    margin-bottom: 12px;
  }
  .modal-input:focus { border-color: var(--accent); }
  .modal-actions { display:flex; gap:8px; justify-content:flex-end; }
  .btn-modal {
    font-family: var(--mono);
    font-size: 9px;
    letter-spacing: 1.5px;
    padding: 7px 14px;
    border-radius: 4px;
    border: 1px solid var(--border2);
    cursor: pointer;
    transition: all .2s;
  }
  .btn-modal.primary {
    background: var(--accent2);
    border-color: var(--accent);
    color: #fff;
  }
  .btn-modal.primary:hover { background: var(--accent); }
  .btn-modal.cancel { background:transparent; color:var(--text2); }
  .btn-modal.cancel:hover { color:var(--text); }

  /* WELCOME */
  .welcome-msg {
    background: linear-gradient(135deg, rgba(0,168,255,0.05), rgba(168,85,247,0.05));
    border: 1px solid rgba(0,168,255,0.15);
    border-radius: var(--radius);
    padding: 20px;
    margin-bottom: 8px;
  }
  .welcome-title {
    font-family: var(--serif);
    font-size: 20px;
    font-weight: 900;
    color: #fff;
    margin-bottom: 6px;
  }
  .welcome-title em { color: var(--gold); font-style:normal; }
  .welcome-desc { font-size: 12px; color: var(--text2); line-height: 1.6; margin-bottom: 14px; }
  .welcome-caps {
    display: flex;
    gap: 6px;
    flex-wrap: wrap;
  }
  .cap-tag {
    font-family: var(--mono);
    font-size: 8px;
    letter-spacing: 1px;
    padding: 3px 8px;
    border-radius: 3px;
    background: var(--surface2);
    border: 1px solid var(--border2);
    color: var(--text2);
  }

  /* TOAST */
  .toast {
    position:fixed; bottom:80px; left:50%; transform:translateX(-50%);
    background: var(--surface);
    border: 1px solid var(--border2);
    color: var(--text);
    padding: 8px 16px;
    border-radius: 20px;
    font-size: 12px;
    font-family: var(--mono);
    letter-spacing: 1px;
    z-index: 200;
    opacity:0; transition:opacity .3s;
    pointer-events:none;
  }
  .toast.show{opacity:1;}

  /* SCROLLBAR */
  .sidebar::-webkit-scrollbar{width:0;}

  /* RESPONSIVE */
  @media(max-width:600px){
    .sidebar{display:none;}
    .quick-actions{padding:8px 12px;}
    .chat-area{padding:14px 12px;}
    .input-area{padding:8px 12px 12px;}
  }
</style>
</head>
<body>

<!-- TOPBAR -->
<div class="topbar">
  <div class="topbar-brand">Blog<em>Fort</em></div>
  <div class="agent-badge">⚡ AGENTE CONTENT</div>
  <div class="topbar-spacer"></div>
  <div class="status-dot" id="statusDot"></div>
  <div class="status-lbl" id="statusLbl">ONLINE</div>
  <button class="api-btn" onclick="openApiModal()">🔑 API KEY</button>
</div>

<!-- SHELL -->
<div class="shell">

  <!-- SIDEBAR -->
  <div class="sidebar">
    <div class="sidebar-section">Módulos</div>
    <div class="nav-item active" onclick="setMode('editor')" id="nav-editor">
      <span class="nav-icon">✍️</span>
      <span class="nav-label">Editor SEO</span>
    </div>
    <div class="nav-item" onclick="setMode('mejorar')" id="nav-mejorar">
      <span class="nav-icon">✨</span>
      <span class="nav-label">Mejorar texto</span>
    </div>
    <div class="nav-item" onclick="setMode('humanizar')" id="nav-humanizar">
      <span class="nav-icon">🧬</span>
      <span class="nav-label">Humanizador</span>
    </div>
    <div class="nav-item" onclick="setMode('corrector')" id="nav-corrector">
      <span class="nav-icon">🔍</span>
      <span class="nav-label">Corrector SEO</span>
    </div>
    <div class="nav-item" onclick="setMode('traductor')" id="nav-traductor">
      <span class="nav-icon">🌐</span>
      <span class="nav-label">Traductor</span>
    </div>
    <div class="nav-item" onclick="setMode('pdf')" id="nav-pdf">
      <span class="nav-icon">📄</span>
      <span class="nav-label">Generador PDF</span>
    </div>
    <div class="nav-item" onclick="setMode('plantillas')" id="nav-plantillas">
      <span class="nav-icon">📋</span>
      <span class="nav-label">Plantillas</span>
    </div>
    <div class="nav-item" onclick="setMode('medico')" id="nav-medico">
      <span class="nav-icon">🔬</span>
      <span class="nav-label">Modo Médico</span>
    </div>

    <div class="sidebar-section" style="margin-top:8px;">Idioma / Dialecto</div>
    <div style="padding:6px 12px;">
      <select id="langSel" onchange="onLangChange()" style="width:100%;background:var(--surface2);border:1px solid var(--border2);color:var(--text);border-radius:4px;padding:6px 8px;font-family:var(--mono);font-size:10px;outline:none;cursor:pointer;">
        <optgroup label="── ESPAÑOL">
          <option value="es-es">🇪🇸 Español (España)</option>
          <option value="es-mx">🇲🇽 Español (México)</option>
          <option value="es-ar">🇦🇷 Español (Argentina)</option>
          <option value="es-co">🇨🇴 Español (Colombia)</option>
          <option value="es-cl">🇨🇱 Español (Chile)</option>
          <option value="es-pe">🇵🇪 Español (Perú)</option>
        </optgroup>
        <optgroup label="── ENGLISH">
          <option value="en-us">🇺🇸 English (US)</option>
          <option value="en-gb">🇬🇧 English (UK)</option>
          <option value="en-au">🇦🇺 English (Australia)</option>
          <option value="en-ca">🇨🇦 English (Canada)</option>
        </optgroup>
        <optgroup label="── PORTUGUÊS">
          <option value="pt-br">🇧🇷 Português (Brasil)</option>
          <option value="pt-pt">🇵🇹 Português (Portugal)</option>
        </optgroup>
        <optgroup label="── FRANÇAIS">
          <option value="fr-fr">🇫🇷 Français (France)</option>
          <option value="fr-ca">🇨🇦 Français (Canada)</option>
          <option value="fr-be">🇧🇪 Français (Belgique)</option>
        </optgroup>
        <optgroup label="── DEUTSCH">
          <option value="de-de">🇩🇪 Deutsch (Deutschland)</option>
          <option value="de-at">🇦🇹 Deutsch (Österreich)</option>
          <option value="de-ch">🇨🇭 Deutsch (Schweiz)</option>
        </optgroup>
        <optgroup label="── ITALIANO">
          <option value="it-it">🇮🇹 Italiano (Italia)</option>
          <option value="it-ch">🇨🇭 Italiano (Svizzera)</option>
        </optgroup>
        <optgroup label="── OTROS">
          <option value="nl-nl">🇳🇱 Nederlands</option>
          <option value="pl-pl">🇵🇱 Polski</option>
        </optgroup>
      </select>
    </div>

    <div class="sidebar-section" style="margin-top:8px;">Sector activo</div>
    <div style="padding:6px 12px;">
      <select id="sectorSel" style="width:100%;background:var(--surface2);border:1px solid var(--border2);color:var(--text);border-radius:4px;padding:6px 8px;font-family:var(--mono);font-size:10px;outline:none;cursor:pointer;">
        <option value="salud_dental">🦷 Salud Dental</option>
        <option value="salud_general">🏥 Salud General</option>
        <option value="nutricion">🥗 Nutrición</option>
        <option value="tecnologia">💻 Tecnología & SaaS</option>
        <option value="finanzas">💰 Finanzas & Legal</option>
        <option value="inmobiliario">🏠 Inmobiliario</option>
        <option value="marketing">📣 Marketing & Agencias</option>
        <option value="ecommerce">🛒 eCommerce</option>
        <option value="educacion">📚 Educación</option>
        <option value="turismo">✈️ Turismo</option>
        <option value="bienestar">🧘 Bienestar</option>
        <option value="legal">⚖️ Legal</option>
        <option value="otros">🏢 Otros</option>
      </select>
    </div>

    <div class="sidebar-footer">
      <div class="quota-bar-wrap">
        <div class="quota-lbl">SESIÓN ACTIVA</div>
        <div class="quota-bar"><div class="quota-fill" id="quotaFill" style="width:0%"></div></div>
      </div>
      <div style="font-family:var(--mono);font-size:8px;color:var(--text3);letter-spacing:1px;" id="tokenCount">0 tokens usados</div>
    </div>
  </div>

  <!-- MAIN -->
  <div class="main">

    <!-- CHAT -->
    <div class="chat-area" id="chatArea">
      <div class="welcome-msg">
        <div class="welcome-title">Agente Blog<em>Fort</em></div>
        <div class="welcome-desc">Motor de Contenido Empresarial con IA. Domino todos los módulos de BlogFort App: Editor SEO, Corrector, Humanizador, Traductor de 22 dialectos, PDF, Plantillas y Modo Médico/Científico con fuentes OMS, NIH y PubMed.</div>
        <div class="welcome-caps">
          <span class="cap-tag">SEO SCORE 0–100</span>
          <span class="cap-tag">HUMAN SCORE</span>
          <span class="cap-tag">MODO MÉDICO</span>
          <span class="cap-tag">22 DIALECTOS</span>
          <span class="cap-tag">13 SECTORES</span>
          <span class="cap-tag">ANTI-MULETILLAS</span>
          <span class="cap-tag">E-E-A-T</span>
          <span class="cap-tag">WORDPRESS EXPORT</span>
        </div>
      </div>
    </div>

    <!-- QUICK ACTIONS -->
    <div class="quick-actions">
      <button class="qa-btn" onclick="quickAction('Escribe un artículo SEO completo con H1, H2, H3 y meta descripción')">✍️ Artículo SEO</button>
      <button class="qa-btn" onclick="quickAction('Humaniza este texto para que suene 100% humano y supere detectores de IA')">🧬 Humanizar</button>
      <button class="qa-btn" onclick="quickAction('Corrige y optimiza el texto con la keyword y mejora el Human Score')">🔍 Corrector SEO</button>
      <button class="qa-btn" onclick="quickAction('Traduce este contenido adaptando el dialecto local del país objetivo')">🌐 Traducir</button>
      <button class="qa-btn" onclick="quickAction('Genera 3 títulos SEO optimizados con la keyword principal')">🎯 Títulos SEO</button>
      <button class="qa-btn" onclick="quickAction('Escribe una meta descripción de exactamente 155 caracteres con la keyword')">📊 Meta Desc</button>
      <button class="qa-btn" onclick="quickAction('Redacta contenido con fuentes OMS, NIH y PubMed en modo médico/científico')">🔬 Modo Médico</button>
      <button class="qa-btn" onclick="quickAction('Genera un documento PDF completo con estructura profesional para este sector')">📄 Generar PDF</button>
    </div>

    <!-- INPUT -->
    <div class="input-area">
      <div class="mode-selector" id="modeSel">
        <div class="mode-pill active" data-mode="escribir" onclick="selectSubMode(this)">ESCRIBIR</div>
        <div class="mode-pill" data-mode="mejorar" onclick="selectSubMode(this)">MEJORAR</div>
        <div class="mode-pill" data-mode="seo" onclick="selectSubMode(this)">SEO</div>
        <div class="mode-pill" data-mode="humanizar" onclick="selectSubMode(this)">HUMANIZAR</div>
        <div class="mode-pill" data-mode="cientifico" onclick="selectSubMode(this)">CIENTÍFICO</div>
        <div class="mode-pill" data-mode="titulo" onclick="selectSubMode(this)">TÍTULOS</div>
        <div class="mode-pill" data-mode="meta" onclick="selectSubMode(this)">META</div>
        <div class="mode-pill" data-mode="conclusion" onclick="selectSubMode(this)">CONCLUSIÓN</div>
        <div class="mode-pill" data-mode="eeat" onclick="selectSubMode(this)">E-E-A-T</div>
        <div class="mode-pill" data-mode="disclaimer" onclick="selectSubMode(this)">DISCLAIMER</div>
      </div>
      <div class="input-wrap">
        <textarea class="chat-input" id="chatInput" placeholder="Describe el artículo, añade el texto a corregir, keyword objetivo, sector, dialecto..." rows="2" onkeydown="handleKey(event)" oninput="autoResize(this)"></textarea>
        <button class="send-btn" id="sendBtn" onclick="sendMessage()">➤</button>
      </div>
      <div class="input-hint">INTRO = enviar · SHIFT+INTRO = nueva línea · Selecciona el módulo con las pastillas de arriba</div>
    </div>

  </div>
</div>

<!-- MODAL API -->
<div class="modal-overlay" id="apiModal">
  <div class="modal">
    <div class="modal-title">🔑 API KEY — ANTHROPIC</div>
    <input type="password" class="modal-input" id="apiInput" placeholder="sk-ant-api03-..." autocomplete="off">
    <div style="font-size:11px;color:var(--text3);margin-bottom:12px;line-height:1.5;">Tu API Key de Anthropic se almacena localmente en este dispositivo. Nunca sale del navegador.</div>
    <div class="modal-actions">
      <button class="btn-modal cancel" onclick="closeApiModal()">CANCELAR</button>
      <button class="btn-modal primary" onclick="saveApiKey()">GUARDAR</button>
    </div>
  </div>
</div>

<!-- TOAST -->
<div class="toast" id="toast"></div>

<script>
// ─── STATE ───────────────────────────────────────────────────────────────────
let apiKey = localStorage.getItem('bf_agent_key') || '';
let currentMode = 'editor';
let currentSubMode = 'escribir';
let totalTokens = 0;
let isLoading = false;
let msgHistory = [];

// ─── SYSTEM PROMPT MAESTRO ───────────────────────────────────────────────────
function buildSystemPrompt() {
  const lang = document.getElementById('langSel').value;
  const sector = document.getElementById('sectorSel').value;

  const dialectNames = {
    'es-es':'español de España (castellano neutro, vosotros)',
    'es-mx':'español de México (ustedes, léxico mexicano)',
    'es-ar':'español de Argentina (vos, léxico rioplatense)',
    'es-co':'español de Colombia (usted formal, léxico colombiano)',
    'es-cl':'español de Chile (léxico chileno, tuteo informal)',
    'es-pe':'español de Perú (léxico peruano, tuteo formal)',
    'en-us':'American English (US spelling, idioms)',
    'en-gb':'British English (UK spelling, -ise endings)',
    'en-au':'Australian English (AU idioms and spelling)',
    'en-ca':'Canadian English (blend of US/UK)',
    'pt-br':'português brasileiro (léxico e ortografia brasileira)',
    'pt-pt':'português europeu (léxico e ortografia de Portugal)',
    'fr-fr':'français de France (registre standard)',
    'fr-ca':'français canadien (québécois, léxico local)',
    'fr-be':'français de Belgique (belgicismes)',
    'de-de':'Deutsch (Deutschland, Hochdeutsch)',
    'de-at':'Deutsch (Österreich, österreichisches Deutsch)',
    'de-ch':'Deutsch (Schweiz, Schweizerhochdeutsch)',
    'it-it':'italiano (Italia standard)',
    'it-ch':'italiano (Svizzera, italiano elvetico)',
    'nl-nl':'Nederlands (standaard Nederlands)',
    'pl-pl':'polski (język standardowy)'
  };
  const sectorNames = {
    salud_dental:'Salud Dental y Odontología',
    salud_general:'Salud General y Medicina',
    nutricion:'Nutrición y Alimentación',
    tecnologia:'Tecnología y SaaS',
    finanzas:'Finanzas y Legal',
    inmobiliario:'Inmobiliario y Real Estate',
    marketing:'Marketing y Agencias Digitales',
    ecommerce:'eCommerce y Tiendas Online',
    educacion:'Educación y Formación',
    turismo:'Turismo y Viajes',
    bienestar:'Bienestar y Lifestyle',
    legal:'Legal y Jurídico',
    otros:'Sector Corporativo General'
  };

  const idioma = dialectNames[lang] || 'español de España';
  const sectorNombre = sectorNames[sector] || 'Sector Corporativo';

  const modeInstructions = {
    escribir: `Redacta contenido original completo con estructura H1 > H2 > H3 clara. Incluye introducción empática, desarrollo con subtemas, datos verificados, pregunta retórica y conclusión con CTA sutil. Mínimo 600 palabras.`,
    mejorar: `Mejora el texto proporcionado: hazlo más fluido, empático y natural. Elimina lenguaje robótico, mejora la estructura y añade transiciones naturales entre párrafos.`,
    seo: `Optimiza para SEO avanzado: integra la keyword de forma natural (densidad 1-2%), estructura con ## H2 y ### H3 claros, añade términos LSI semánticos relacionados, verifica que keyword aparezca en el primer párrafo, en al menos un H2 y en la conclusión.`,
    humanizar: `Reescribe el texto completamente para que suene 100% humano. Usa primera persona cuando sea natural. Frases cortas alternadas con frases más largas. Lenguaje cálido pero profesional. Elimina absolutamente cualquier cliché de IA. Objetivo: superar 90/100 en detectores de IA.`,
    cientifico: `Desarrolla el tema con rigor científico total: incluye al menos 3 datos con fuentes reales entre corchetes [Fuente: Organismo, Año], usa terminología clínica o técnica correcta con explicación accesible entre paréntesis, estructura el argumento con evidencia progresiva.`,
    titulo: `Genera exactamente 5 títulos SEO con la keyword. Formato: numerados del 1 al 5. Cada título entre 50-60 caracteres. Tipos: informacional, pregunta, listado, guía, emocional. Deben sonar naturales y atractivos para humanos.`,
    meta: `Escribe UNA meta descripción SEO de EXACTAMENTE 155 caracteres con la keyword incluida. Sin comillas. Tono humano y persuasivo. Incluye un verbo de acción y el beneficio principal.`,
    conclusion: `Redacta una conclusión completa de 3-4 párrafos: resumen empático, reflexión personal o dato final de impacto, y CTA sutil que invite a la acción sin ser comercial agresivo.`,
    eeat: `Reescribe o mejora aplicando E-E-A-T completo para SEO de Google: señales de Experiencia real (casos, situaciones vividas), Expertise técnica (terminología precisa), Autoridad (citas de organismos reconocidos), Confianza (datos verificables, disclaimers cuando necesario).`,
    disclaimer: `Redacta un disclaimer profesional en 2-3 líneas. Debe indicar que el contenido es informativo, que no sustituye consulta profesional especializada, y recomienda acudir a un profesional cualificado del sector ${sectorNombre}.`
  };

  const antiMuletillas = `
REGLAS ANTI-MULETILLAS ABSOLUTAS — NUNCA uses estas frases:
"en resumen", "es importante destacar", "cabe destacar", "sin duda alguna", "en el mundo actual", "en los últimos años", "es fundamental", "hoy en día", "en la era digital", "cada vez más", "no cabe duda", "es crucial", "juega un papel fundamental", "en definitiva", "en conclusión" (salvo en la conclusión real), "como mencionamos anteriormente", "vale la pena mencionar", "es interesante notar", "a lo largo de este artículo".
`;

  const humanScore = `
REGLA DE ORO — HUMAN SCORE MÍNIMO 85/100:
- Alterna frases cortas (bajo 15 palabras) con frases más largas (hasta 25 palabras)
- Usa conectores conversacionales: "y es que...", "lo curioso es que...", "la realidad es..."
- Incluye al menos una pregunta retórica
- Una analogía o metáfora accesible
- Evita repetir la misma estructura de párrafo más de 2 veces seguidas
- Nunca empieces dos párrafos consecutivos con la misma palabra
`;

  const medicalSources = (sector === 'salud_dental' || sector === 'salud_general' || sector === 'nutricion' || sector === 'bienestar') ? `
MODO MÉDICO/CIENTÍFICO:
- Eres un redactor médico con rigor científico y experiencia clínica real
- Fuentes OBLIGATORIAS al hacer afirmaciones clínicas: [Fuente: OMS/WHO, Año], [Fuente: NIH, Año], [Fuente: PubMed, Año], [Fuente: ADA, Año], [Fuente: Cochrane, Año], [Fuente: Mayo Clinic, Año], [Fuente: MedlinePlus, Año]
- NUNCA inventes datos, estadísticas ni fuentes. Si no tienes certeza de una cifra exacta, exprésala cualitativamente
- E-E-A-T médico: cada párrafo debe demostrar experiencia, expertise, autoridad y confianza
` : '';

  return `Eres el Agente BlogFort, el motor de contenido empresarial con IA de BlogFort Agency LLC. Dominas al 100% todos los módulos de BlogFort App y produces contenido de calidad excepcional para clientes empresariales.

CONTEXTO OPERATIVO:
- Idioma de respuesta: ${idioma}
- Sector activo: ${sectorNombre}
- Modo de generación: ${currentSubMode.toUpperCase()}

INSTRUCCIÓN ESPECÍFICA PARA ESTE MODO:
${modeInstructions[currentSubMode] || modeInstructions.escribir}

${antiMuletillas}
${humanScore}
${medicalSources}

FORMATO DE RESPUESTA:
1. Primero: el contenido solicitado directamente, con toda la estructura necesaria
2. Después del contenido: una línea separadora ---
3. Métricas estimadas en este formato exacto:
   📊 SEO Score: X/100 | Human Score: X/100 | Palabras: X | Keyword density: X%
4. Si detectas mejoras clave posibles: máximo 2 sugerencias en una línea

REGLAS ABSOLUTAS:
- Responde DIRECTAMENTE con el contenido. Sin preámbulos como "Aquí te presento...", "Por supuesto...", "Claro que sí..."
- Si el usuario no especifica keyword, infiere la más lógica para el sector y úsala
- Si el usuario pide humanizar, el resultado debe ser irreconocible como IA
- Calidad profesional en cada respuesta. Nunca contenido mediocre.
- Nunca rompas el carácter de agente especializado en contenido empresarial`;
}

// ─── API CALL ─────────────────────────────────────────────────────────────────
async function callAPI(userMessage) {
  if (!apiKey) { openApiModal(); throw new Error('API Key requerida'); }

  msgHistory.push({ role: 'user', content: userMessage });

  const response = await fetch('https://api.anthropic.com/v1/messages', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'x-api-key': apiKey, 'anthropic-version': '2023-06-01', 'anthropic-dangerous-direct-browser-access': 'true' },
    body: JSON.stringify({
      model: 'claude-sonnet-5',
      max_tokens: 4000,
      system: buildSystemPrompt(),
      messages: msgHistory.slice(-10)
    })
  });

  if (!response.ok) {
    const err = await response.json().catch(() => ({}));
    throw new Error(err.error?.message || `Error ${response.status}`);
  }

  const data = await response.json();
  const assistantMsg = data.content?.[0]?.text || '';
  msgHistory.push({ role: 'assistant', content: assistantMsg });

  // tokens
  const used = data.usage?.input_tokens + data.usage?.output_tokens || 0;
  totalTokens += used;
  updateTokenCount();

  return assistantMsg;
}

// ─── SEND MESSAGE ─────────────────────────────────────────────────────────────
async function sendMessage(overrideText) {
  if (isLoading) return;
  const input = document.getElementById('chatInput');
  const text = overrideText || input.value.trim();
  if (!text) return;

  input.value = '';
  input.style.height = 'auto';

  appendMsg('user', text);
  const typingId = appendTyping();
  setLoading(true);

  try {
    const reply = await callAPI(text);
    removeTyping(typingId);
    appendAgentMsg(reply);
  } catch(e) {
    removeTyping(typingId);
    appendMsg('agent', `⚠️ ${e.message}`);
    if (e.message.includes('401') || e.message.includes('key')) openApiModal();
  }

  setLoading(false);
}

function quickAction(text) {
  document.getElementById('chatInput').value = text;
  sendMessage();
}

// ─── DOM HELPERS ──────────────────────────────────────────────────────────────
function appendMsg(role, text) {
  const area = document.getElementById('chatArea');
  const div = document.createElement('div');
  div.className = `msg ${role === 'user' ? 'user' : 'agent'}`;

  const avatar = role === 'user' ? '👤' : '⚡';
  const label = role === 'user' ? 'TÚ' : 'AGENTE BLOGFORT';

  div.innerHTML = `
    <div class="msg-avatar">${avatar}</div>
    <div class="msg-bubble">
      <div class="msg-meta">${label}</div>
      <div class="msg-content">${escHtml(text)}</div>
    </div>`;
  area.appendChild(div);
  area.scrollTop = area.scrollHeight;
}

function appendAgentMsg(raw) {
  const area = document.getElementById('chatArea');

  // Separar contenido de métricas
  const parts = raw.split('---');
  const content = parts[0].trim();
  const metrics = parts[1] ? parts[1].trim() : '';

  const div = document.createElement('div');
  div.className = 'msg agent';

  // Parse métricas
  let metricsHTML = '';
  if (metrics) {
    const seoM = metrics.match(/SEO Score:\s*(\d+)/i);
    const humM = metrics.match(/Human Score:\s*(\d+)/i);
    const wordsM = metrics.match(/Palabras:\s*(\d+)/i);
    const kwM = metrics.match(/Keyword density:\s*([\d.]+%)/i);

    const seo = seoM ? parseInt(seoM[1]) : null;
    const hum = humM ? parseInt(humM[1]) : null;

    if (seo !== null || hum !== null) {
      const seoClass = seo >= 70 ? 'green' : seo >= 50 ? 'yellow' : 'red';
      const humClass = hum >= 80 ? 'green' : hum >= 60 ? 'yellow' : 'red';
      metricsHTML = `
        <div class="seo-score-strip">
          ${seo !== null ? `<span class="seo-pill ${seoClass}">SEO ${seo}/100</span>` : ''}
          ${hum !== null ? `<span class="seo-pill ${humClass}">HUMAN ${hum}/100</span>` : ''}
          ${wordsM ? `<span class="seo-pill green">${wordsM[1]} palabras</span>` : ''}
          ${kwM ? `<span class="seo-pill yellow">KW ${kwM[1]}</span>` : ''}
        </div>`;
    }
  }

  div.innerHTML = `
    <div class="msg-avatar">⚡</div>
    <div class="msg-bubble" style="max-width:100%;">
      <div class="msg-meta">AGENTE BLOGFORT</div>
      <div class="output-block">
        <div class="output-header">✦ CONTENIDO GENERADO — ${currentSubMode.toUpperCase()}</div>
        <div class="output-body" id="out-${Date.now()}">${escHtml(content)}</div>
        ${metricsHTML ? `<div style="padding:8px 14px;">${metricsHTML}</div>` : ''}
        <div class="output-actions">
          <button class="out-btn primary" onclick="copyOutput(this)">📋 Copiar</button>
          <button class="out-btn" onclick="copyHTML(this)">🟢 HTML WordPress</button>
          <button class="out-btn" onclick="dispatchEmail(this)">📧 Email</button>
          <button class="out-btn" onclick="dispatchWhatsApp(this)">💬 WhatsApp</button>
          <button class="out-btn" onclick="dispatchPDF(this)">📄 PDF</button>
        </div>
      </div>
    </div>`;

  area.appendChild(div);
  area.scrollTop = area.scrollHeight;
}

function appendTyping() {
  const area = document.getElementById('chatArea');
  const id = 'typing-' + Date.now();
  const div = document.createElement('div');
  div.className = 'msg agent';
  div.id = id;
  div.innerHTML = `
    <div class="msg-avatar">⚡</div>
    <div class="msg-bubble">
      <div class="msg-meta">AGENTE BLOGFORT</div>
      <div class="typing"><span></span><span></span><span></span></div>
    </div>`;
  area.appendChild(div);
  area.scrollTop = area.scrollHeight;
  return id;
}

function removeTyping(id) {
  const el = document.getElementById(id);
  if (el) el.remove();
}

function copyOutput(btn) {
  const body = btn.closest('.output-block').querySelector('.output-body');
  navigator.clipboard.writeText(body.textContent).then(() => toast('✅ Copiado al portapapeles'));
}

function copyHTML(btn) {
  const body = btn.closest('.output-block').querySelector('.output-body');
  const text = body.textContent;
  // Convertir markdown básico a HTML limpio
  let html = text
    .replace(/^# (.+)$/gm, '<h1>$1</h1>')
    .replace(/^## (.+)$/gm, '<h2>$1</h2>')
    .replace(/^### (.+)$/gm, '<h3>$1</h3>')
    .replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
    .replace(/\*(.+?)\*/g, '<em>$1</em>')
    .replace(/\n\n/g, '</p><p>')
    .replace(/^(?!<h[123]>)(.+)$/gm, (m) => m.startsWith('<') ? m : `<p>${m}</p>`);
  navigator.clipboard.writeText(html).then(() => toast('🟢 HTML limpio copiado para WordPress'));
}

function escHtml(t) {
  return t.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
}

// ─── MODES ───────────────────────────────────────────────────────────────────
function setMode(mode) {
  currentMode = mode;
  document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
  const el = document.getElementById('nav-' + mode);
  if (el) el.classList.add('active');

  // Mapear modo sidebar a submodo
  const modeMap = {
    editor: 'escribir', mejorar: 'mejorar', humanizar: 'humanizar',
    corrector: 'seo', traductor: 'escribir', pdf: 'escribir',
    plantillas: 'escribir', medico: 'cientifico'
  };
  const sub = modeMap[mode] || 'escribir';
  selectSubModeByValue(sub);

  // Actualizar placeholder
  const placeholders = {
    editor: 'Keyword principal, sector, título del artículo...',
    mejorar: 'Pega aquí el texto que quieres mejorar...',
    humanizar: 'Pega el texto generado por IA para humanizarlo...',
    corrector: 'Texto a corregir + keyword objetivo...',
    traductor: 'Texto a traducir + idioma y dialecto de destino...',
    pdf: 'Describe el documento PDF que necesitas generar...',
    plantillas: 'Indica el tipo de plantilla y sector...',
    medico: 'Tema médico/científico con fuentes verificadas...'
  };
  document.getElementById('chatInput').placeholder = placeholders[mode] || 'Escribe tu instrucción...';
}

function selectSubMode(el) {
  document.querySelectorAll('.mode-pill').forEach(p => p.classList.remove('active'));
  el.classList.add('active');
  currentSubMode = el.dataset.mode;
}

function selectSubModeByValue(val) {
  document.querySelectorAll('.mode-pill').forEach(p => {
    p.classList.toggle('active', p.dataset.mode === val);
  });
  currentSubMode = val;
}

// ─── UI HELPERS ───────────────────────────────────────────────────────────────
function setLoading(on) {
  isLoading = on;
  document.getElementById('sendBtn').disabled = on;
  const dot = document.getElementById('statusDot');
  const lbl = document.getElementById('statusLbl');
  if (on) {
    dot.style.background = 'var(--gold)';
    dot.style.boxShadow = '0 0 8px var(--gold)';
    lbl.style.color = 'var(--gold)';
    lbl.textContent = 'PROCESANDO';
  } else {
    dot.style.background = 'var(--green)';
    dot.style.boxShadow = '0 0 8px var(--green)';
    lbl.style.color = 'var(--green)';
    lbl.textContent = 'ONLINE';
  }
}

function updateTokenCount() {
  document.getElementById('tokenCount').textContent = `${totalTokens.toLocaleString()} tokens usados`;
  const pct = Math.min((totalTokens / 50000) * 100, 100);
  document.getElementById('quotaFill').style.width = pct + '%';
}

function autoResize(el) {
  el.style.height = 'auto';
  el.style.height = Math.min(el.scrollHeight, 120) + 'px';
}

function handleKey(e) {
  if (e.key === 'Enter' && !e.shiftKey) {
    e.preventDefault();
    sendMessage();
  }
}

function toast(msg, dur=2500) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), dur);
}

// ─── API MODAL ────────────────────────────────────────────────────────────────
function openApiModal() {
  document.getElementById('apiInput').value = apiKey ? '••••••••' : '';
  document.getElementById('apiModal').classList.add('on');
  setTimeout(() => document.getElementById('apiInput').focus(), 100);
}

function closeApiModal() {
  document.getElementById('apiModal').classList.remove('on');
}

function saveApiKey() {
  const val = document.getElementById('apiInput').value.trim();
  if (val && val !== '••••••••') {
    apiKey = val;
    localStorage.setItem('bf_agent_key', apiKey);
    toast('✅ API Key guardada');
  }
  closeApiModal();
  updateStatus();
}

function updateStatus() {
  const dot = document.getElementById('statusDot');
  const lbl = document.getElementById('statusLbl');
  if (apiKey) {
    dot.style.background = 'var(--green)';
    lbl.textContent = 'ONLINE';
  } else {
    dot.style.background = 'var(--red)';
    dot.style.boxShadow = '0 0 8px var(--red)';
    lbl.style.color = 'var(--red)';
    lbl.textContent = 'SIN API KEY';
  }
}

// ─── INIT ─────────────────────────────────────────────────────────────────────
document.addEventListener('DOMContentLoaded', () => {
  updateStatus();
  if (!apiKey) {
    setTimeout(() => {
      appendMsg('agent', '⚠️ Para activar el Agente BlogFort necesitas configurar tu API Key de Anthropic.\n\nHaz clic en el botón 🔑 API KEY en la esquina superior derecha.');
    }, 500);
  } else {
    setTimeout(() => {
      appendMsg('agent', 'Agente BlogFort operativo ✅\n\nDomino los 8 módulos de BlogFort App al 100%: Editor SEO, Corrector/Humanizador, Traductor con 22 dialectos, PDF con IA, Plantillas sectoriales y Modo Médico/Científico con fuentes OMS, NIH y PubMed.\n\nSelecciona el módulo en la barra izquierda, elige el submodo arriba, y dame la instrucción. Trabajo en 13 sectores y 6 idiomas.\n\n¿Por dónde empezamos?');
    }, 400);
  }
});

// Cerrar modal al click fuera
document.getElementById('apiModal').addEventListener('click', (e) => {
  if (e.target === e.currentTarget) closeApiModal();
});

// ─── SINCRONIZACIÓN AUTOMÁTICA DE IDIOMA ─────────────────────────────────────
function onLangChange() {
  const lang = document.getElementById('langSel').value;
  const base = lang.split('-')[0];
  // Sincroniza el modo activo si es corrector/humanizador
  if (currentMode === 'corrector' || currentMode === 'humanizar' || currentMode === 'mejorar') {
    toast('🌐 Idioma sincronizado: ' + document.getElementById('langSel').selectedOptions[0].text);
  }
  // Actualizar placeholder del input según dialecto
  const input = document.getElementById('chatInput');
  if (currentMode === 'traductor') {
    input.placeholder = 'Pega el texto a traducir — dialecto destino ya configurado: ' +
      document.getElementById('langSel').selectedOptions[0].text;
  }
}

// ─── DESPACHO OMNICANAL ───────────────────────────────────────────────────────
function dispatchEmail(btn) {
  const body = btn.closest('.output-block').querySelector('.output-body');
  const text = body.textContent.trim();
  const subject = encodeURIComponent('Contenido BlogFort Agency');
  const bodyEnc = encodeURIComponent(text.substring(0, 2000));
  window.open('mailto:?subject=' + subject + '&body=' + bodyEnc);
  toast('📧 Abriendo cliente de email...');
}

function dispatchWhatsApp(btn) {
  const body = btn.closest('.output-block').querySelector('.output-body');
  const text = body.textContent.trim();
  const encoded = encodeURIComponent(text.substring(0, 4096));
  window.open('https://wa.me/?text=' + encoded, '_blank');
  toast('💬 Abriendo WhatsApp...');
}

function dispatchPDF(btn) {
  const body = btn.closest('.output-block').querySelector('.output-body');
  const text = body.textContent.trim();
  const sector = document.getElementById('sectorSel').selectedOptions[0].text;
  const fecha = new Date().toLocaleDateString('es-ES');

  const html = `<!DOCTYPE html><html><head><meta charset="UTF-8">
  <style>
    body{font-family:Georgia,serif;max-width:800px;margin:40px auto;padding:0 40px;color:#1a1a2e;line-height:1.8;}
    h1{color:#0a3d62;font-size:26px;border-bottom:3px solid #f0a500;padding-bottom:12px;}
    h2{color:#1a1a2e;font-size:20px;margin-top:28px;}
    h3{color:#2c3e50;font-size:16px;}
    .meta{font-size:11px;color:#888;margin-bottom:30px;font-family:monospace;}
    .footer{margin-top:50px;padding-top:16px;border-top:1px solid #ddd;font-size:10px;color:#aaa;text-align:center;}
    p{margin:12px 0;}
  </style></head><body>
  <div class="meta">BlogFort Agency LLC · ${sector} · ${fecha}</div>
  <div>${text.replace(/\n/g,'<br>').replace(/# (.+)/g,'<h1>$1</h1>').replace(/## (.+)/g,'<h2>$1</h2>').replace(/### (.+)/g,'<h3>$1</h3>')}</div>
  <div class="footer">Generado por Agente BlogFort · blogfort.agency</div>
  </body></html>`;

  const win = window.open('', '_blank');
  win.document.write(html);
  win.document.close();
  setTimeout(() => { win.print(); }, 600);
  toast('📄 PDF listo para imprimir/guardar');
}
</script>
</body>
</html>
